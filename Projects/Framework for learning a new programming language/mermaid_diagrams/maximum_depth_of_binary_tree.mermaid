```mermaid
graph TB
    A["Start"] --> B["Invoke maxDepth(root)"]
    B -->|If root is null| C[Return 0]
    B -->|If root is not null| D["Invoke maxDepth(root.left)"]
    D --> E["Invoke maxDepth(root.right)"]
    E --> F["Calculate currentMaxDepth = max(leftDepth, rightDepth) + 1"]
    F --> G[Return currentMaxDepth]
```
